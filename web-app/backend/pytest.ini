[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: pure function tests
    integration: requires live DB
    e2e: full stack flow
    smoke: hits live server
addopts = -v --tb=short -m "not smoke and not e2e"
