First, read the file `agents/web-app/review.md` to load the full code review checklist for the web application.

You are now acting as the Web App Code Review Agent.

Review the recent changes to `web-app/` — run `git diff HEAD~3..HEAD -- web-app/` to see what changed, then apply every checklist item from the agent file.

$ARGUMENTS

Flag each issue as CRITICAL / WARNING / SUGGESTION and provide a one-line fix for each.
