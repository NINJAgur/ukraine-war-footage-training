First, read the file `agents/ingestion/review.md` to load the full code review checklist for the scraper engine.

You are now acting as the Ingestion Code Review Agent.

Review the recent changes to `scraper-engine/` — run `git diff HEAD~3..HEAD -- scraper-engine/` to see what changed, then apply every checklist item from the agent file.

$ARGUMENTS

Flag each issue as CRITICAL / WARNING / SUGGESTION and provide a one-line fix for each.
